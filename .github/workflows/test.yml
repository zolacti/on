# Trigger the workflow on push or pull request
on: push
name: Test Zola CLI
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
      with:
        path: actions
    - uses: ./actions    
    - uses: ./actions